{
  "private": true,
  "name": "processing",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:server": "cd packages/server && yarn start:watch",
    "start:shared": "cd packages/shared && yarn build:watch",
    "start:client": "cd packages/client && yarn dev"
  },
  "dependencies": {
    "concurrently": "^7.3.0"
  }
}
